## JWT Authentication System

Implement login/signup endpoints that generate JWT tokens. Create middleware to verify JWT from Authorization header. Include a protected route that extracts user info from JWT. Implement token expiration handling.