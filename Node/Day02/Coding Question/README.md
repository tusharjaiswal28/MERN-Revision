## Basic Authentication Middleware

Create an Express middleware that implements Basic Authentication. Create protected routes that require authentication. Include login endpoint that sets cookies and middleware to verify them on subsequent requests.